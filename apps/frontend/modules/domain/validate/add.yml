
fields:
  name:
    required:
      msg: Name field is required.
    sfRegexValidator:
      match:        Yes
      match_error:  Only letters, numbers, dots, underscore and dash allowed
      pattern:      /^[0-9,a-z,\.,\-,_]+$/i



